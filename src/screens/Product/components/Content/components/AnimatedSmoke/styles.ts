import { Canvas } from '@shopify/react-native-skia'
import styled from 'styled-components/native'

export const AnimatedSmokeContainer = styled.View``

export const StyledCanvas = styled(Canvas)`
  height: 137px;
  width: 64px;
  margin-bottom: -65px;
`
